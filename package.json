{
  "name": "design-system",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.8.1",
  "devDependencies": {
    "turbo": "^2.0.9"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "check-types": "tsc --noEmit"
  }
}
